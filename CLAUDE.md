# Project instructions — Dempsey Golf Tracker

## Git commits and pushes

**Never run `git commit` or `git push` without the user's explicit permission for that specific
change.** The user wants to review code themselves before it enters history. Staging files
(`git add`) or preparing a commit message for review is fine — do not execute the commit or push
until the user explicitly says to.
